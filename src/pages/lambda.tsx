import { LambdaPageLayout } from 'lambda/Lamba.styled';
import { Lambda } from 'lambda/Lambda';
import React from 'react';

export const LambdaPage = () => {
  return (
    <LambdaPageLayout>
      <Lambda />
    </LambdaPageLayout>
  );
};
